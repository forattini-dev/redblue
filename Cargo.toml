[package]
name = "redblue"
version = "0.1.0"
edition = "2021"
authors = ["RedBlue Security Team"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

[profile.dev]
opt-level = 1

[dependencies]
# ZERO DEPENDENCIES - NEVER ADD ANYTHING HERE
# Everything is implemented from scratch using only Rust std library
# This is a core principle of redblue - see CLAUDE.md and AGENTS.md
