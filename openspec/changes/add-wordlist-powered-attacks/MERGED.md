# Merged into add-tool-parity-roadmap

This proposal has been merged into `add-tool-parity-roadmap` on 2025-12-08.

## Merged Items

The following sections were added to the tool-parity roadmap:

1. **Section 1.3 - Subdomain Brute-Force (DNS-based)**: 16 tasks
   - DNS resolver pool, wildcard detection, multi-resolver support
   - CLI: `rb recon subdomain bruteforce <domain> -w <wordlist>`

2. **Section 2.1.20-2.1.24 - Web Fuzzing CLI**: 5 tasks
   - Wire existing fuzzer module to CLI
   - CLI: `rb web fuzz <url> -w <wordlist>`

3. **Section 3.4 - Credential Testing Module**: 20 tasks
   - HTTP Basic/Digest, form-based, SSH/FTP/SMTP auth testing
   - CLI: `rb auth test <target> -u <userlist> -p <passlist>`

4. **Section 4.2 - Wordlist Management**: 16 tasks
   - Tar extraction, stats, filters, mutations, generation
   - CLI: `rb wordlist info <file>`

## Total Tasks Added: 57

See `../add-tool-parity-roadmap/tasks.md` for the complete task list.
